@extends('layouts.app')

@section('content')
<div class="container">
    <div class="row justify-content-center">
        <div class="col-12">
        	OK
{{--         	@if(session('success'))
				{{session('success')}}
			@endif
			<br>
			@if(session('order_id'))
				Ваш заказ №: {{session('order_id')}}				
			@endif
			<br>
			<a href="{{route('shop')}}" class="btn-main text-center">Продолжить покупки</a>	 --}}
        </div>
    </div>
</div>
@endsection